{
  "name": "pet-names-server",
  "version": "1.0.0",
  "description": "Jax Pet Names API",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "seed": "knex seed:run",
    "migrate": "knex migrate:up",
    "build": "npm install && knex migrate:up --env production",
    "reset_build": "npm install && knex migrate:up --env production && knex seed:run --env production",
    "server": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "2.4.2",
    "pg": "^8.10.0"
  }
}
